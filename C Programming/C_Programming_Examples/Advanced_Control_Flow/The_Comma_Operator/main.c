#include <stdio.h>

int main()
{

    int i, sum;

    while ( i < 100 )
        sum += data[i], ++i;

    int i(5, 10); // 10 is assigned to i

    int j = ( f1(), f2() ); // The rsult of the 2nd expression is assigned to j

    x = ( y = 3, ( z = ++y + 2 ) + 5 ); // x = 11

    for ( i = 0, j = 100; i != 10; ++i, j -= 10 );

    printf("Stephen"),
    printf("Sottosanti"),
    printf("Done");

    days = 360,365;
    days = 360;
    365;

    days = (360, 365);

    char ch, date;
    int a = 1, b = 2;

    printf("%d:%d", x, y);

    void fun(x,y);

    return 1;
}